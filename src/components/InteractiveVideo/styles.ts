import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	video: { flex: 1 },
	actionsContainer: { position: 'absolute', width: '100%', height: '100%' },
	iconContainer: {
		backgroundColor: 'white',
		borderRadius: 20,
		width: 40,
		height: 40,
		display: 'flex',
		alignItems: 'center',
		justifyContent: 'center',
	},
});
